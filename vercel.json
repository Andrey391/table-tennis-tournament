{
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "buildCommand": "prisma generate --schema=server/prisma/schema.prisma && npm run build -w client",
  "framework": null,
  "regions": ["dub1"],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
