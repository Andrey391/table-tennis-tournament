// "Players" and "Rating" were the same list twice — same rows, same avatars, one
// sorted by rating and one searchable. There is now a single list (RatingPage);
// this route stays so older links keep working.
export { default } from "./RatingPage";
